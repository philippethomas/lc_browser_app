.row-fluid
  .span7
    - var x = (total < size) ? total : size
      p#summary.lead Showing 1 through #{x} of #{total}

  .span5
    form#csv(action='/csvExport', method='post')
      button.btn.pull-right#csv Export CSV

    #pager


script
  var numPages = Math.ceil(#{total}/#{size});
  $('#pager').bootpag({
    total: numPages,
    page: 1,
    maxVisible: 5,
    leaps:false
  });
